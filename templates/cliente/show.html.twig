{% extends 'base.html.twig' %}

{% block title %}Cliente{% endblock %}

{% block body %}
    <h1>Cliente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcliente</th>
                <td>{{ cliente.idcliente }}</td>
            </tr>
            <tr>
                <th>NombreApellido</th>
                <td>{{ cliente.nombreApellido }}</td>
            </tr>
            <tr>
                <th>Dirección</th>
                <td>{{ cliente.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ cliente.telefono }}</td>
            </tr>
            <tr>
                <th>Codigopostal</th>
                <td>{{ cliente.codigopostal }}</td>
            </tr>
            <tr>
                <th>FechaDeNacimiento</th>
                <td>{{ cliente.fechaDeNacimiento ? cliente.fechaDeNacimiento|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Provincia</th>
                <td>{{ cliente.provincia }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cliente_index') }}">back to list</a>

    <a href="{{ path('app_cliente_edit', {'idcliente': cliente.idcliente}) }}">edit</a>

    {{ include('cliente/_delete_form.html.twig') }}
{% endblock %}
